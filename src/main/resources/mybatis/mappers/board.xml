<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">
	<insert id="insert" parameterType="boardVo">
	INSERT INTO board
	(no, title, content, hit, reg_date, user_no)
	VALUES(
		seq_board_pk.nextval,
		#{title},
		#{content},
		0,
		sysdate,
		#{userNo}
	)
	</insert>
	
	<update id="update" parameterType="long">
	<!-- TODO WHERE조건은 넣어야 하는가?  -->
	UPDATE board
	SET title=#{title}, 
	content=#{content}
	WHERE no = #{userNo}
	</update>
	
	<select id="selectOne" parameterType="long" resultType="boardVo">
	SELECT 
	    no,
	    title,
	    content,
	    reg_date as regDate,
	    user_no as userNo
	FROM board 
	WHERE no = #{no}
	</select>
	
	<select id="selectAll" resultType="boardVo">
	SELECT b.no, title, content, hit, reg_date as regDate, 
			user_no as userNo, name as userName
	FROM board b, users u
	WHERE b.user_no = u.no
	</select>
</mapper>